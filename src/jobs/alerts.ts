// Usage alerts at 80%/100% - TODO stretch. Retries + failure alert required.
import { notImplemented } from '../services/meter';

export async function checkAlerts(): Promise<never> {
  return notImplemented('stretch');
}
