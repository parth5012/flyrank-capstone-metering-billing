// BillingService.sync - TODO Phase 3. Verified webhooks only; dedup via stripe_events.
import { notImplemented } from './meter';

export async function syncSubscription(): Promise<never> {
  return notImplemented(3);
}
