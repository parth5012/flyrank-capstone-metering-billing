// tenants/plans/subscriptions repo - TODO Phase 2/3. Tenant isolation required.
import { notImplemented } from '../services/meter';

export async function findTenant(): Promise<never> {
  return notImplemented(2);
}
