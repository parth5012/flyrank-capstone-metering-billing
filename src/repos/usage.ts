// usage_events repo - TODO Phase 2. Every query filters tenant_id.
import { notImplemented } from '../services/meter';

export async function insertUsageEvent(): Promise<never> {
  return notImplemented(2);
}
